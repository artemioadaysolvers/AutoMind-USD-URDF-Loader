AutoMind MJCF Viewer BUILD173 validation

Fixed startup exception reported from remote commit 3d235a6:
- animate() runs before MJCF is assigned; keepGridInfiniteForView no longer calls bounds calculation while robot is null.
- getObjectBounds() is a non-throwing manual mesh traversal and does not invoke THREE.Box3.setFromObject(null).
- helper grid is only frozen by refreshRobotContext(validModel), never by render loop.
- resize uses initial immutable model span once captured, not current articulated bounds.

Static validation:
- node --check passed for every .js file in MJCF_Viewer.
