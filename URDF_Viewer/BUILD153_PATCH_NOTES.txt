BUILD153_SyntaxSafeNoLineComments

Fixes the jsDelivr import SyntaxError caused by line-comment corruption/collapsing in URDF_Viewer/urdfplus_viewer_main.js after publishing.

Changes:
- Re-emitted all JavaScript modules with comments removed using the TypeScript compiler, preserving ESM syntax.
- In particular, urdfplus_viewer_main.js no longer contains line comments that can escape into executable text if a hosting/upload path rewrites newlines.
- Keeps BUILD152 functionality: unified URDF+ pipeline, texture inlining, thumbnail rebuild, joint drag, USD-style joint/loop batches, grid/model sizing, orthographic grid safety, render-mode-aware show all, and advanced ComponentsPanel/ToolsDock.

Validation:
- node --check passed for every .js file.
- zip integrity checked.
