BUILD153_EXACT_HTML_JOINTS_TWEEN_OVERLAYS

- Active URDF+ entrypoint now runs the uploaded standalone HTML mechanism directly:
  URDFPlus_Viewer_Standalone_BUILD153.html.
- This preserves the exact BUILD138 internal joint system from the reference HTML:
  realtime constrained drag, loop/coupling solver calls, pinned final settle, and the same pointer delta math.
- Added a module/iframe bridge so Colab/PowerBI can still call render(opts) with urdfContent, meshDB/assetDB, or URDF_Zip.
- Component-panel click now isolates the selected component with an opacity TWEEN instead of hard visibility changes.
- Show Joints and Show Loops now fade out with the same TWEEN during component isolation and fade back in on Show all if their toggles are enabled.
- Legacy URDFLoader fallback remains available with opts.forceLegacyURDFLoader=true and now also suppresses mechanism overlays during isolate.
